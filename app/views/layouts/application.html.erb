<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>StockSuno</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= stylesheet_link_tag 'https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css', rel: 'stylesheet' %>
</head>
<body class="bg-gradient-to-br from-yellow-100 to-orange-200 font-sans leading-normal tracking-normal min-h-screen flex flex-col">
  <!-- Header -->
  <header class="bg-gradient-to-r from-orange-500 to-red-500 text-white py-4 shadow-lg">
    <div class="container mx-auto flex justify-between items-center px-6">
      <h1 class="text-3xl font-extrabold">StockSuno</h1>
      <nav class="hidden md:flex space-x-6">
        <%= link_to "News", root_path, class: "hover:underline transition duration-300 ease-in-out transform hover:scale-105" %>
        <%= link_to "About Us", about_path, class: "hover:underline transition duration-300 ease-in-out transform hover:scale-105" %>
        <%= link_to "FAQ", faq_path, class: "hover:underline transition duration-300 ease-in-out transform hover:scale-105" %>
        <%= link_to "Contact Us", contact_path, class: "hover:underline transition duration-300 ease-in-out transform hover:scale-105" %>
      </nav>
      <button class="md:hidden text-white focus:outline-none" id="menu-button">
        <i class="fas fa-bars"></i>
      </button>
    </div>
  </header>

  <!-- Mobile Menu -->
  <div class="md:hidden bg-gradient-to-r from-orange-500 to-red-500 text-white py-4 shadow-lg hidden" id="mobile-menu">
    <nav class="space-y-2 px-6">
      <%= link_to "News", root_path, class: "block hover:underline" %>
      <%= link_to "About Us", about_path, class: "block hover:underline" %>
      <%= link_to "FAQ", faq_path, class: "block hover:underline" %>
      <%= link_to "Contact Us", contact_path, class: "block hover:underline" %>
    </nav>
  </div>

  <!-- Main Content Area -->
  <main class="flex-grow flex items-center justify-center p-4 bg-white m-5 rounded-lg shadow-2xl">
    <%= yield %>
  </main>

  <!-- Footer -->
  <footer class="bg-gradient-to-r from-orange-500 to-red-500 text-white py-4 mt-auto">
    <div class="container mx-auto text-center">
      <p>&copy; <%= Time.now.year %> StockSuno. All rights reserved.</p>
    </div>
  </footer>

  <!-- Scripts -->
  <script>
    document.getElementById('menu-button').addEventListener('click', function() {
      var menu = document.getElementById('mobile-menu');
      menu.classList.toggle('hidden');
    });
  </script>
</body>
</html>
